{
  "name": "aimweb",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host localhost --port 4000",
    "build": "tsc && vite build",
    "preview": "vite preview --host localhost --port 4000"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/three": "^0.180.0",
    "typescript": "^5.0.0",
    "vite": "^8.0.0"
  }
}
